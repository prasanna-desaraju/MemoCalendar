# Interactive Wall Calendar (React)

A responsive React implementation of the wall-calendar challenge with:
- Wall-calendar inspired hero section
- Start/end day range selector with highlighted in-between days
- Integrated notes panel with month-wise `localStorage` persistence
- Mobile + desktop responsive layout

## Run Locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
